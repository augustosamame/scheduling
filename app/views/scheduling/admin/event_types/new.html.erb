<% content_for :title, "New Event Type" %>

<div class="mb-6">
  <div class="flex items-center justify-between">
    <div>
      <h2 class="text-2xl font-bold text-gray-900">Create New Event Type</h2>
      <p class="mt-1 text-sm text-gray-500">Set up a new appointment type for your schedule</p>
    </div>
    <%= link_to "← Back to Event Types", admin_event_types_path, class: "px-4 py-2 text-sm font-medium text-gray-700 bg-white rounded-md border border-gray-300 hover:bg-gray-50" %>
  </div>
</div>

<%= render 'form' %>
